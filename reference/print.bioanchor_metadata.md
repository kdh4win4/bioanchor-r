# Print method for bioanchor_metadata

Print method for bioanchor_metadata

## Usage

``` r
# S3 method for class 'bioanchor_metadata'
print(x, ...)
```

## Arguments

- x:

  A `bioanchor_metadata` object.

- ...:

  Additional arguments (ignored).

## Value

`x` invisibly.
