# Validate BioAnchor Metadata

Checks that a metadata object has the required fields and valid
diagnostics.

## Usage

``` r
validate_metadata(metadata)
```

## Arguments

- metadata:

  A `bioanchor_metadata` object.

## Value

`TRUE` invisibly if valid; errors otherwise.
