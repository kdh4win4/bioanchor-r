% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{extract_metadata}
\alias{extract_metadata}
\title{Extract MCMC Metadata from Fitted Model Objects}
\usage{
extract_metadata(fit, model_name = NULL, extra = list())
}
\arguments{
\item{fit}{A fitted model object. Supported classes:
\itemize{
\item \code{stanfit} (from rstan)
\item \code{brmsfit} (from brms)
\item \code{mcmc.list} (from coda)
\item \code{draws_array}, \code{draws_df}, \code{draws_list} (from posterior)
}}

\item{model_name}{Optional character string naming the model.}

\item{extra}{A named list of additional metadata to include.}
}
\value{
A list with class \code{"bioanchor_metadata"} containing:
\itemize{
\item \code{model_name}: character
\item \code{backend}: character (e.g., "rstan", "brms", "coda", "posterior")
\item \code{timestamp}: ISO 8601 timestamp
\item \code{diagnostics}: list with Rhat, ESS, divergences (where available)
\item \code{summary}: posterior summary data frame
\item \code{model_info}: model-specific information
\item \code{extra}: user-supplied metadata
}
}
\description{
Extracts convergence diagnostics, posterior summaries, and model
specifications from various MCMC backends (Stan, brms, coda).
}
\examples{
# With a posterior draws object:
library(posterior)
draws <- example_draws("eight_schools")
meta <- extract_metadata(draws, model_name = "eight_schools")
}
